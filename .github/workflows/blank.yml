name: Keep App Alive

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Hugging Face Space
        run: curl -sSf -H "Authorization: Bearer ${{ secrets.HF_TOKEN }}" 'https://huggingface.co/spaces/luoguocheng/n8n-free'

